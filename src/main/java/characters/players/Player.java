package characters.players;

import characters.Character;

public class Player extends Character {
    // Placeholder for additional controlled player functionality.
    // Initialisation

    public Player(String name, int health) {
        super(name, health);
    }
}
